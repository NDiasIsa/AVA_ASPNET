@model AVA_ASPNET.Models.TurmaViewModel
@{
    ViewData["Title"] = "Editar Turma";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2 class="titulo mb-4">Editar Turma</h2>
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <form asp-action="EditarTurma" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />
                        <div asp-validation-summary="All" class="alert alert-danger"
                             style="display:@(ViewData.ModelState.ErrorCount > 0 ? "block" : "none")"></div>
                        <div class="mb-3">
                            <label asp-for="Codigo" class="form-label"></label>
                            <input asp-for="Codigo" class="form-control" />
                            <span asp-validation-for="Codigo" class="text-danger small"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Descricao" class="form-label"></label>
                            <input asp-for="Descricao" class="form-control" />
                        </div>
                        <div class="mb-4">
                            <label asp-for="Ano" class="form-label"></label>
                            <select asp-for="Ano" class="form-select">
                                <option value="1">1º EM</option>
                                <option value="2">2º EM</option>
                                <option value="3">3º EM</option>
                            </select>
                        </div>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-circle me-1"></i> Salvar
                            </button>
                            <a asp-controller="Admin" asp-action="Turmas" class="btn btn-outline-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>